import { expect } from 'chai';
import Game from "./Game";

export default function() {

    describe('Start a simple game', function() {
        it('should start a bowling game', async function() {
            const game = new Game();
            expect
        });
    });
}